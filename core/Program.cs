using System;
using DiseaseCore;

namespace coursework
{
    class Program
    {
        static void Main(string[] args)
        {
            // This package must be used as a separate module instead as an
            // entrypoint
        }
    }
}
